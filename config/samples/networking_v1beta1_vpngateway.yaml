apiVersion: networking.waycloak.io/v1beta1
kind: VPNGateway
metadata:
  name: example
  namespace: media
spec:
  gatewayClassName: gluetun.waycloak.io
  allowedRoutes:
    namespaces:
      from: Same
  clusterTraffic:
    mode: TunnelAll
  dns:
    mode: Gateway
