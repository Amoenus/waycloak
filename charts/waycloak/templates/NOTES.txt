The clean-break Waycloak replacement API has been installed in namespace
{{ .Release.Namespace }}.

This #128 development slice installs only the v1beta1 CRDs, persona/controller
RBAC, controller identity, and binding admission defense. It intentionally does
not start the alpha controller or admission webhooks.

Do not enroll workloads yet. Replacement route reconciliation, UID-bound
bindings, node programming, and CNI-capable scheduling land in dependency order
in #129-#136. Until those gates pass, this chart is an API validation artifact,
not a stable product installation.
