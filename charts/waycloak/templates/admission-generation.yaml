apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "waycloak.admissionGenerationConfigMap" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "waycloak.labels" . | nindent 4 }}
data:
  generation: {{ include "waycloak.admissionGeneration" . | quote }}
