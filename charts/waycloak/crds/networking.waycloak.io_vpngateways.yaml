---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: vpngateways.networking.waycloak.io
spec:
  group: networking.waycloak.io
  names:
    kind: VPNGateway
    listKind: VPNGatewayList
    plural: vpngateways
    shortNames:
    - vpngw
    singular: vpngateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Accepted')].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterTraffic:
                properties:
                  cidrs:
                    description: CIDRs remain on the CNI main routing table in Preserve
                      mode.
                    items:
                      format: cidr
                      maxLength: 18
                      type: string
                    maxItems: 64
                    minItems: 1
                    type: array
                    x-kubernetes-validations:
                    - message: cluster CIDRs must be IPv4
                      rule: self.all(c, !c.contains(':'))
                  mode:
                    enum:
                    - Preserve
                    - Gateway
                    - Deny
                    type: string
                type: object
                x-kubernetes-validations:
                - message: Preserve mode requires at least one cluster CIDR
                  rule: '!has(self.mode) || self.mode != ''Preserve'' || (has(self.cidrs)
                    && size(self.cidrs) > 0)'
                - message: cluster CIDRs are only valid in Preserve mode
                  rule: '!has(self.cidrs) || size(self.cidrs) == 0 || (has(self.mode)
                    && self.mode == ''Preserve'')'
              dns:
                properties:
                  mode:
                    type: string
                type: object
              engine:
                properties:
                  config:
                    properties:
                      envFrom:
                        description: |-
                          EnvFrom imports engine-native non-secret environment variables. Waycloak
                          validates reserved integration keys but never copies values into the Pod specification.
                        items:
                          description: |-
                            LocalObjectReference contains enough information to let you locate the
                            referenced object inside the same namespace.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        maxItems: 8
                        minItems: 1
                        type: array
                      files:
                        description: Files mounts operator-owned ConfigMaps or Secrets
                          only into the engine.
                        items:
                          properties:
                            configMapRef:
                              description: |-
                                LocalObjectReference contains enough information to let you locate the
                                referenced object inside the same namespace.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            mountPath:
                              pattern: ^/.*
                              type: string
                            secretRef:
                              description: |-
                                LocalObjectReference contains enough information to let you locate the
                                referenced object inside the same namespace.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - mountPath
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of configMapRef or secretRef is required
                            rule: has(self.configMapRef) != has(self.secretRef)
                        maxItems: 8
                        type: array
                    required:
                    - envFrom
                    type: object
                  image:
                    type: string
                  type:
                    type: string
                required:
                - type
                type: object
              overlay:
                properties:
                  cidr:
                    format: cidr
                    type: string
                  mtu:
                    default: 1320
                    format: int32
                    minimum: 576
                    type: integer
                  vni:
                    format: int32
                    maximum: 16777215
                    minimum: 1
                    type: integer
                required:
                - cidr
                - vni
                type: object
              portForwarding:
                properties:
                  driver:
                    enum:
                    - ProtonNatPmp
                    type: string
                  enabled:
                    type: boolean
                type: object
                x-kubernetes-validations:
                - message: enabled port forwarding requires driver ProtonNatPmp
                  rule: '!has(self.enabled) || !self.enabled || (has(self.driver)
                    && self.driver == ''ProtonNatPmp'')'
              provider:
                properties:
                  credentialsSecretRef:
                    description: |-
                      LocalObjectReference contains enough information to let you locate the
                      referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  name:
                    type: string
                  protocol:
                    type: string
                  region:
                    type: string
                required:
                - credentialsSecretRef
                - name
                type: object
              workloadAccess:
                properties:
                  namespaceSelector:
                    description: |-
                      A label selector is a label query over a set of resources. The result of matchLabels and
                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                      label selector matches no objects.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - namespaceSelector
                type: object
            required:
            - engine
            - overlay
            - workloadAccess
            type: object
            x-kubernetes-validations:
            - message: exactly one of legacy provider or engine.config is required
              rule: has(self.provider) != has(self.engine.config)
          status:
            properties:
              clientCount:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              overlay:
                description: |-
                  GatewayOverlayStatus contains observed data-plane state. Endpoint is an IP
                  address and UDP port suitable for netip.ParseAddrPort.
                properties:
                  appliedMembershipGeneration:
                    type: string
                  desiredMembershipGeneration:
                    type: string
                  endpoint:
                    type: string
                  healthPort:
                    format: int32
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
