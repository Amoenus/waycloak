"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""

import regex
import k8s.apimachinery.pkg.apis.meta.v1

_regex_match = regex.match

schema PortForwardLease:
    r"""
    networking waycloak io v1beta1 port forward lease

    Attributes
    ----------
    apiVersion : str, default is "networking.waycloak.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "PortForwardLease", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingWaycloakIoV1beta1PortForwardLeaseSpec, default is Undefined, required
        spec
    status : NetworkingWaycloakIoV1beta1PortForwardLeaseStatus, default is Undefined, optional
        status
    """
    apiVersion: "networking.waycloak.io/v1beta1" = "networking.waycloak.io/v1beta1"

    kind: "PortForwardLease" = "PortForwardLease"

    metadata?: v1.ObjectMeta
    spec: NetworkingWaycloakIoV1beta1PortForwardLeaseSpec

    status?: NetworkingWaycloakIoV1beta1PortForwardLeaseStatus

schema NetworkingWaycloakIoV1beta1PortForwardLeaseSpec:
    r"""
    networking waycloak io v1beta1 port forward lease spec

    Attributes
    ----------
    applicationAdapterRef : NetworkingWaycloakIoV1beta1PortForwardLeaseSpecApplicationAdapterRef, default is Undefined, optional
        application adapter ref
    backendRef : NetworkingWaycloakIoV1beta1PortForwardLeaseSpecBackendRef, default is Undefined, required
        backend ref
    endpointPolicy : str, default is "SingleActive", optional
        endpoint policy
    gatewayRef : NetworkingWaycloakIoV1beta1PortForwardLeaseSpecGatewayRef, default is Undefined, required
        gateway ref
    protocols : [str], default is Undefined, required
        protocols
    """
    applicationAdapterRef?: NetworkingWaycloakIoV1beta1PortForwardLeaseSpecApplicationAdapterRef
    backendRef: NetworkingWaycloakIoV1beta1PortForwardLeaseSpecBackendRef

    endpointPolicy?: "SingleActive" = "SingleActive"
    gatewayRef: NetworkingWaycloakIoV1beta1PortForwardLeaseSpecGatewayRef

    protocols: [str]

    check:
        len(protocols) >= 1
        len(protocols) <= 2

schema NetworkingWaycloakIoV1beta1PortForwardLeaseSpecApplicationAdapterRef:
    r"""
    networking waycloak io v1beta1 port forward lease spec application adapter ref

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    """
    name: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")

schema NetworkingWaycloakIoV1beta1PortForwardLeaseSpecBackendRef:
    r"""
    networking waycloak io v1beta1 port forward lease spec backend ref

    Attributes
    ----------
    group : str, default is Undefined, required
        APIGroup is empty for core APIs or an RFC 1123 subdomain for named groups.
    kind : str, default is Undefined, required
        Kind is a Kubernetes API kind.
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    port : int | str, default is Undefined, required
        port
    """
    group: str

    kind: str

    name: str

    port: int | str

    check:
        len(group) <= 253
        _regex_match(str(group), r"^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(kind) <= 63
        len(kind) >= 1
        _regex_match(str(kind), r"^[A-Za-z]([-A-Za-z0-9]*[A-Za-z0-9])?$")
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")

schema NetworkingWaycloakIoV1beta1PortForwardLeaseSpecGatewayRef:
    r"""
    networking waycloak io v1beta1 port forward lease spec gateway ref

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    namespace : str, default is Undefined, required
        NamespaceName is a Kubernetes namespace name.
    """
    name: str

    namespace: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(namespace) <= 63
        len(namespace) >= 1
        _regex_match(str(namespace), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?$")

schema NetworkingWaycloakIoV1beta1PortForwardLeaseStatus:
    r"""
    networking waycloak io v1beta1 port forward lease status

    Attributes
    ----------
    activeEndpoint : NetworkingWaycloakIoV1beta1PortForwardLeaseStatusActiveEndpoint, default is Undefined, optional
        active endpoint
    conditions : [NetworkingWaycloakIoV1beta1PortForwardLeaseStatusConditionsItems0], default is Undefined, optional
        Conditions implements the common positive-polarity status contract.
    handoffGeneration : int, default is Undefined, optional
        handoff generation
    observedGeneration : int, default is Undefined, optional
        observed generation
    provider : NetworkingWaycloakIoV1beta1PortForwardLeaseStatusProvider, default is Undefined, optional
        provider
    """
    activeEndpoint?: NetworkingWaycloakIoV1beta1PortForwardLeaseStatusActiveEndpoint
    conditions?: [NetworkingWaycloakIoV1beta1PortForwardLeaseStatusConditionsItems0]
    handoffGeneration?: int
    observedGeneration?: int
    provider?: NetworkingWaycloakIoV1beta1PortForwardLeaseStatusProvider

    check:
        len(conditions) <= 32 if conditions

schema NetworkingWaycloakIoV1beta1PortForwardLeaseStatusActiveEndpoint:
    r"""
    networking waycloak io v1beta1 port forward lease status active endpoint

    Attributes
    ----------
    endpointSliceUID : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    phase : str, default is Undefined, required
        phase
    podUID : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    serviceUID : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    """
    endpointSliceUID: str

    phase: "Selecting" | "Draining" | "Active"

    podUID: str

    serviceUID: str

    check:
        len(endpointSliceUID) <= 128
        len(endpointSliceUID) >= 1
        _regex_match(str(endpointSliceUID), r"^[^[:space:]]+$")
        len(podUID) <= 128
        len(podUID) >= 1
        _regex_match(str(podUID), r"^[^[:space:]]+$")
        len(serviceUID) <= 128
        len(serviceUID) >= 1
        _regex_match(str(serviceUID), r"^[^[:space:]]+$")

schema NetworkingWaycloakIoV1beta1PortForwardLeaseStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """
    lastTransitionTime: str

    message: str

    observedGeneration?: int
    reason: str

    status: "True" | "False" | "Unknown"

    $type: str

    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")

schema NetworkingWaycloakIoV1beta1PortForwardLeaseStatusProvider:
    r"""
    networking waycloak io v1beta1 port forward lease status provider

    Attributes
    ----------
    expiresAt : str, default is Undefined, required
        expires at
    publicAddress : str, default is Undefined, required
        public address
    publicPort : int, default is Undefined, required
        public port
    """
    expiresAt: str

    publicAddress: str

    publicPort: int

    check:
        len(publicAddress) <= 253
        len(publicAddress) >= 1
        publicPort <= 65535
        publicPort >= 1
