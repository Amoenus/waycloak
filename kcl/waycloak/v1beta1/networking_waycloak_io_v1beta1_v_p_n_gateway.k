"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""

import regex
import k8s.apimachinery.pkg.apis.meta.v1

_regex_match = regex.match

schema VPNGateway:
    r"""
    networking waycloak io v1beta1 v p n gateway

    Attributes
    ----------
    apiVersion : str, default is "networking.waycloak.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "VPNGateway", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingWaycloakIoV1beta1VPNGatewaySpec, default is Undefined, required
        spec
    status : NetworkingWaycloakIoV1beta1VPNGatewayStatus, default is Undefined, optional
        status
    """
    apiVersion: "networking.waycloak.io/v1beta1" = "networking.waycloak.io/v1beta1"

    kind: "VPNGateway" = "VPNGateway"

    metadata?: v1.ObjectMeta
    spec: NetworkingWaycloakIoV1beta1VPNGatewaySpec

    status?: NetworkingWaycloakIoV1beta1VPNGatewayStatus

schema NetworkingWaycloakIoV1beta1VPNGatewaySpec:
    r"""
    networking waycloak io v1beta1 v p n gateway spec

    Attributes
    ----------
    allowedRoutes : NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutes, default is Undefined, optional
        allowed routes
    clusterTraffic : NetworkingWaycloakIoV1beta1VPNGatewaySpecClusterTraffic, default is Undefined, required
        cluster traffic
    credentialRefs : [NetworkingWaycloakIoV1beta1VPNGatewaySpecCredentialRefsItems0], default is Undefined, optional
        credential refs
    dns : NetworkingWaycloakIoV1beta1VPNGatewaySpecDNS, default is Undefined, optional
        dns
    gatewayClassName : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    nativeConfigRefs : [NetworkingWaycloakIoV1beta1VPNGatewaySpecNativeConfigRefsItems0], default is Undefined, optional
        native config refs
    placement : NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacement, default is Undefined, optional
        placement
    requestedFeatures : [str], default is Undefined, optional
        requested features
    """
    allowedRoutes?: NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutes
    clusterTraffic: NetworkingWaycloakIoV1beta1VPNGatewaySpecClusterTraffic

    credentialRefs?: [NetworkingWaycloakIoV1beta1VPNGatewaySpecCredentialRefsItems0]
    dns?: NetworkingWaycloakIoV1beta1VPNGatewaySpecDNS
    gatewayClassName: str

    nativeConfigRefs?: [NetworkingWaycloakIoV1beta1VPNGatewaySpecNativeConfigRefsItems0]
    placement?: NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacement
    requestedFeatures?: [str]

    check:
        len(credentialRefs) <= 16 if credentialRefs
        len(gatewayClassName) <= 253
        len(gatewayClassName) >= 1
        _regex_match(str(gatewayClassName), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(nativeConfigRefs) <= 16 if nativeConfigRefs
        len(requestedFeatures) <= 64 if requestedFeatures
        all i in requestedFeatures {len(i) <= 253 if i not in [None, Undefined]} if requestedFeatures
        all i in requestedFeatures {len(i) >= 1 if i not in [None, Undefined]} if requestedFeatures
        all i in requestedFeatures {_regex_match(str(i), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$") if i} if requestedFeatures

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutes:
    r"""
    networking waycloak io v1beta1 v p n gateway spec allowed routes

    Attributes
    ----------
    namespaces : NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespaces, default is Undefined, optional
        namespaces
    """
    namespaces?: NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespaces

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespaces:
    r"""
    networking waycloak io v1beta1 v p n gateway spec allowed routes namespaces

    Attributes
    ----------
    from : str, default is "Same", optional
        from
    selector : NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelector, default is Undefined, optional
        selector
    """
    from?: "Same" | "Selector" | "All" = "Same"
    selector?: NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelector

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelector:
    r"""
    A label selector is a label query over a set of resources. The result of matchLabels and
    matchExpressions are ANDed. An empty label selector matches all objects. A null
    label selector matches no objects.

    Attributes
    ----------
    matchExpressions : [NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
        map is equivalent to an element of matchExpressions, whose key field is "key", the
        operator is "In", and the values array contains only "value". The requirements are ANDed.
    """
    matchExpressions?: [NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelectorMatchExpressionsItems0]
    matchLabels?: {str:str}

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecAllowedRoutesNamespacesSelectorMatchExpressionsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """
    key: str

    operator: str

    values?: [str]

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecClusterTraffic:
    r"""
    networking waycloak io v1beta1 v p n gateway spec cluster traffic

    Attributes
    ----------
    bypassCIDRs : [str], default is Undefined, optional
        bypass c ID rs
    mode : str, default is Undefined, required
        mode
    """
    bypassCIDRs?: [str]
    mode: "BypassCluster" | "TunnelAll"

    check:
        len(bypassCIDRs) <= 64 if bypassCIDRs

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecCredentialRefsItems0:
    r"""
    networking waycloak io v1beta1 v p n gateway spec credential refs items0

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    role : str, default is Undefined, required
        QualifiedName is a Kubernetes qualified name.
    """
    name: str

    role: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(role) <= 253
        len(role) >= 1
        _regex_match(str(role), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$")

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecDNS:
    r"""
    networking waycloak io v1beta1 v p n gateway spec DNS

    Attributes
    ----------
    mode : str, default is "Gateway", optional
        mode
    """
    mode?: "Gateway" = "Gateway"

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecNativeConfigRefsItems0:
    r"""
    networking waycloak io v1beta1 v p n gateway spec native config refs items0

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    role : str, default is Undefined, required
        QualifiedName is a Kubernetes qualified name.
    """
    name: str

    role: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(role) <= 253
        len(role) >= 1
        _regex_match(str(role), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$")

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacement:
    r"""
    networking waycloak io v1beta1 v p n gateway spec placement

    Attributes
    ----------
    nodeSelector : {str:str}, default is Undefined, optional
        node selector
    tolerations : [NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacementTolerationsItems0], default is Undefined, optional
        tolerations
    """
    nodeSelector?: {str:str}
    tolerations?: [NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacementTolerationsItems0]

    check:
        len(tolerations) <= 32 if tolerations

schema NetworkingWaycloakIoV1beta1VPNGatewaySpecPlacementTolerationsItems0:
    r"""
    The pod this Toleration is attached to tolerates any taint that matches
    the triple <key,value,effect> using the matching operator <operator>.

    Attributes
    ----------
    effect : str, default is Undefined, optional
        Effect indicates the taint effect to match. Empty means match all taint effects.
        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
    key : str, default is Undefined, optional
        Key is the taint key that the toleration applies to. Empty means match all taint keys.
        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
    operator : str, default is Undefined, optional
        Operator represents a key's relationship to the value.
        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
        Exists is equivalent to wildcard for value, so that a pod can
        tolerate all taints of a particular category.
        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
    tolerationSeconds : int, default is Undefined, optional
        TolerationSeconds represents the period of time the toleration (which must be
        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
        it is not set, which means tolerate the taint forever (do not evict). Zero and
        negative values will be treated as 0 (evict immediately) by the system.
    value : str, default is Undefined, optional
        Value is the taint value the toleration matches to.
        If the operator is Exists, the value should be empty, otherwise just a regular string.
    """
    effect?: str
    key?: str
    operator?: str
    tolerationSeconds?: int
    value?: str

schema NetworkingWaycloakIoV1beta1VPNGatewayStatus:
    r"""
    networking waycloak io v1beta1 v p n gateway status

    Attributes
    ----------
    addresses : [NetworkingWaycloakIoV1beta1VPNGatewayStatusAddressesItems0], default is Undefined, optional
        addresses
    conditions : [NetworkingWaycloakIoV1beta1VPNGatewayStatusConditionsItems0], default is Undefined, optional
        Conditions implements the common positive-polarity status contract.
    gatewayClass : NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClass, default is Undefined, optional
        gateway class
    observedGeneration : int, default is Undefined, optional
        observed generation
    supportedFeatures : [str], default is Undefined, optional
        supported features
    """
    addresses?: [NetworkingWaycloakIoV1beta1VPNGatewayStatusAddressesItems0]
    conditions?: [NetworkingWaycloakIoV1beta1VPNGatewayStatusConditionsItems0]
    gatewayClass?: NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClass
    observedGeneration?: int
    supportedFeatures?: [str]

    check:
        len(addresses) <= 16 if addresses
        len(conditions) <= 32 if conditions
        len(supportedFeatures) <= 64 if supportedFeatures
        all i in supportedFeatures {len(i) <= 253 if i not in [None, Undefined]} if supportedFeatures
        all i in supportedFeatures {len(i) >= 1 if i not in [None, Undefined]} if supportedFeatures
        all i in supportedFeatures {_regex_match(str(i), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$") if i} if supportedFeatures

schema NetworkingWaycloakIoV1beta1VPNGatewayStatusAddressesItems0:
    r"""
    networking waycloak io v1beta1 v p n gateway status addresses items0

    Attributes
    ----------
    $type : str, default is Undefined, required
        QualifiedName is a Kubernetes qualified name.
    value : str, default is Undefined, required
        value
    """
    $type: str

    value: str

    check:
        len($type) <= 253
        len($type) >= 1
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$")
        len(value) <= 253
        len(value) >= 1

schema NetworkingWaycloakIoV1beta1VPNGatewayStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """
    lastTransitionTime: str

    message: str

    observedGeneration?: int
    reason: str

    status: "True" | "False" | "Unknown"

    $type: str

    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")

schema NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClass:
    r"""
    networking waycloak io v1beta1 v p n gateway status gateway class

    Attributes
    ----------
    controllerName : str, default is Undefined, required
        ControllerName identifies one immutable controller implementation.
    releaseIdentity : NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClassReleaseIdentity, default is Undefined, required
        release identity
    """
    controllerName: str

    releaseIdentity: NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClassReleaseIdentity

    check:
        len(controllerName) <= 253
        len(controllerName) >= 1
        _regex_match(str(controllerName), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)+/[A-Za-z0-9][A-Za-z0-9/._~-]*$")

schema NetworkingWaycloakIoV1beta1VPNGatewayStatusGatewayClassReleaseIdentity:
    r"""
    networking waycloak io v1beta1 v p n gateway status gateway class release identity

    Attributes
    ----------
    manifestDigest : str, default is Undefined, required
        manifest digest
    version : str, default is Undefined, required
        version
    """
    manifestDigest: str

    version: str

    check:
        _regex_match(str(manifestDigest), r"^sha256:[a-f0-9]{64}$")
        len(version) <= 128
        len(version) >= 1
