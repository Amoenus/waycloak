"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""

import regex
import k8s.apimachinery.pkg.apis.meta.v1

_regex_match = regex.match

schema VPNGatewayClass:
    r"""
    networking waycloak io v1beta1 v p n gateway class

    Attributes
    ----------
    apiVersion : str, default is "networking.waycloak.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "VPNGatewayClass", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingWaycloakIoV1beta1VPNGatewayClassSpec, default is Undefined, required
        spec
    status : NetworkingWaycloakIoV1beta1VPNGatewayClassStatus, default is Undefined, optional
        status
    """
    apiVersion: "networking.waycloak.io/v1beta1" = "networking.waycloak.io/v1beta1"

    kind: "VPNGatewayClass" = "VPNGatewayClass"

    metadata?: v1.ObjectMeta
    spec: NetworkingWaycloakIoV1beta1VPNGatewayClassSpec

    status?: NetworkingWaycloakIoV1beta1VPNGatewayClassStatus

schema NetworkingWaycloakIoV1beta1VPNGatewayClassSpec:
    r"""
    networking waycloak io v1beta1 v p n gateway class spec

    Attributes
    ----------
    conformanceProfile : str, default is Undefined, required
        QualifiedName is a Kubernetes qualified name.
    controllerName : str, default is Undefined, required
        ControllerName identifies one immutable controller implementation.
    parametersRef : NetworkingWaycloakIoV1beta1VPNGatewayClassSpecParametersRef, default is Undefined, optional
        parameters ref
    releaseIdentity : NetworkingWaycloakIoV1beta1VPNGatewayClassSpecReleaseIdentity, default is Undefined, required
        release identity
    supportedFeatures : [str], default is Undefined, required
        supported features
    """
    conformanceProfile: str

    controllerName: str

    parametersRef?: NetworkingWaycloakIoV1beta1VPNGatewayClassSpecParametersRef
    releaseIdentity: NetworkingWaycloakIoV1beta1VPNGatewayClassSpecReleaseIdentity

    supportedFeatures: [str]

    check:
        len(conformanceProfile) <= 253
        len(conformanceProfile) >= 1
        _regex_match(str(conformanceProfile), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$")
        len(controllerName) <= 253
        len(controllerName) >= 1
        _regex_match(str(controllerName), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)+/[A-Za-z0-9][A-Za-z0-9/._~-]*$")
        len(supportedFeatures) >= 6
        len(supportedFeatures) <= 64
        all i in supportedFeatures {len(i) <= 253 if i not in [None, Undefined]} if supportedFeatures
        all i in supportedFeatures {len(i) >= 1 if i not in [None, Undefined]} if supportedFeatures
        all i in supportedFeatures {_regex_match(str(i), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?([A-Za-z0-9][-A-Za-z0-9_.]{0,61})?[A-Za-z0-9]$") if i} if supportedFeatures

schema NetworkingWaycloakIoV1beta1VPNGatewayClassSpecParametersRef:
    r"""
    ParametersRef cannot target a Secret and must name a cluster-scoped implementation input.

    Attributes
    ----------
    group : str, default is Undefined, required
        APIGroup is empty for core APIs or an RFC 1123 subdomain for named groups.
    kind : str, default is Undefined, required
        Kind is a Kubernetes API kind.
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    """
    group: str

    kind: str

    name: str

    check:
        len(group) <= 253
        _regex_match(str(group), r"^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(kind) <= 63
        len(kind) >= 1
        _regex_match(str(kind), r"^[A-Za-z]([-A-Za-z0-9]*[A-Za-z0-9])?$")
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")

schema NetworkingWaycloakIoV1beta1VPNGatewayClassSpecReleaseIdentity:
    r"""
    networking waycloak io v1beta1 v p n gateway class spec release identity

    Attributes
    ----------
    manifestDigest : str, default is Undefined, required
        manifest digest
    version : str, default is Undefined, required
        version
    """
    manifestDigest: str

    version: str

    check:
        _regex_match(str(manifestDigest), r"^sha256:[a-f0-9]{64}$")
        len(version) <= 128
        len(version) >= 1

schema NetworkingWaycloakIoV1beta1VPNGatewayClassStatus:
    r"""
    networking waycloak io v1beta1 v p n gateway class status

    Attributes
    ----------
    conditions : [NetworkingWaycloakIoV1beta1VPNGatewayClassStatusConditionsItems0], default is Undefined, optional
        Conditions implements the common positive-polarity status contract.
    observedGeneration : int, default is Undefined, optional
        observed generation
    """
    conditions?: [NetworkingWaycloakIoV1beta1VPNGatewayClassStatusConditionsItems0]
    observedGeneration?: int

    check:
        len(conditions) <= 32 if conditions

schema NetworkingWaycloakIoV1beta1VPNGatewayClassStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """
    lastTransitionTime: str

    message: str

    observedGeneration?: int
    reason: str

    status: "True" | "False" | "Unknown"

    $type: str

    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")
