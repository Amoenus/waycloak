"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""

import regex
import k8s.apimachinery.pkg.apis.meta.v1

_regex_match = regex.match

schema VPNWorkloadBinding:
    r"""
    networking waycloak io v1beta1 v p n workload binding

    Attributes
    ----------
    apiVersion : str, default is "networking.waycloak.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "VPNWorkloadBinding", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpec, default is Undefined, required
        spec
    status : NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatus, default is Undefined, optional
        status
    """
    apiVersion: "networking.waycloak.io/v1beta1" = "networking.waycloak.io/v1beta1"

    kind: "VPNWorkloadBinding" = "VPNWorkloadBinding"

    metadata?: v1.ObjectMeta
    spec: NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpec

    status?: NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatus

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpec:
    r"""
    networking waycloak io v1beta1 v p n workload binding spec

    Attributes
    ----------
    allocation : NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecAllocation, default is Undefined, required
        allocation
    gatewayRef : NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecGatewayRef, default is Undefined, required
        gateway ref
    nodeName : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    podRef : NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecPodRef, default is Undefined, required
        pod ref
    routeRef : NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecRouteRef, default is Undefined, required
        route ref
    """
    allocation: NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecAllocation

    gatewayRef: NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecGatewayRef

    nodeName: str

    podRef: NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecPodRef

    routeRef: NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecRouteRef

    check:
        _regex_match(str(nodeName), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(nodeName) <= 253
        len(nodeName) >= 1
        len(nodeName) <= 253
        len(nodeName) >= 1

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecAllocation:
    r"""
    networking waycloak io v1beta1 v p n workload binding spec allocation

    Attributes
    ----------
    address : str, default is Undefined, required
        address
    identity : str, default is Undefined, required
        identity
    """
    address: str

    identity: str

    check:
        len(identity) <= 128
        len(identity) >= 1

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecGatewayRef:
    r"""
    networking waycloak io v1beta1 v p n workload binding spec gateway ref

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    namespace : str, default is Undefined, required
        NamespaceName is a Kubernetes namespace name.
    uid : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    """
    name: str

    namespace: str

    uid: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(namespace) <= 63
        len(namespace) >= 1
        _regex_match(str(namespace), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?$")
        len(uid) <= 128
        len(uid) >= 1
        _regex_match(str(uid), r"^[^[:space:]]+$")

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecPodRef:
    r"""
    networking waycloak io v1beta1 v p n workload binding spec pod ref

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    uid : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    """
    name: str

    uid: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(uid) <= 128
        len(uid) >= 1
        _regex_match(str(uid), r"^[^[:space:]]+$")

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingSpecRouteRef:
    r"""
    networking waycloak io v1beta1 v p n workload binding spec route ref

    Attributes
    ----------
    name : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    uid : str, default is Undefined, required
        ObjectUID is the exact UID of a Kubernetes object.
    """
    name: str

    uid: str

    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(uid) <= 128
        len(uid) >= 1
        _regex_match(str(uid), r"^[^[:space:]]+$")

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatus:
    r"""
    networking waycloak io v1beta1 v p n workload binding status

    Attributes
    ----------
    agent : NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusAgent, default is Undefined, optional
        agent
    appliedGeneration : int, default is Undefined, optional
        applied generation
    conditions : [NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusConditionsItems0], default is Undefined, optional
        Conditions implements the common positive-polarity status contract.
    observedGatewayUID : str, default is Undefined, optional
        ObjectUID is the exact UID of a Kubernetes object.
    observedGeneration : int, default is Undefined, optional
        observed generation
    observedPodUID : str, default is Undefined, optional
        ObjectUID is the exact UID of a Kubernetes object.
    """
    agent?: NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusAgent
    appliedGeneration?: int
    conditions?: [NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusConditionsItems0]
    observedGatewayUID?: str
    observedGeneration?: int
    observedPodUID?: str

    check:
        len(conditions) <= 32 if conditions
        len(observedGatewayUID) <= 128 if observedGatewayUID not in [None, Undefined]
        len(observedGatewayUID) >= 1 if observedGatewayUID not in [None, Undefined]
        _regex_match(str(observedGatewayUID), r"^[^[:space:]]+$") if observedGatewayUID
        len(observedPodUID) <= 128 if observedPodUID not in [None, Undefined]
        len(observedPodUID) >= 1 if observedPodUID not in [None, Undefined]
        _regex_match(str(observedPodUID), r"^[^[:space:]]+$") if observedPodUID

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusAgent:
    r"""
    networking waycloak io v1beta1 v p n workload binding status agent

    Attributes
    ----------
    instanceID : str, default is Undefined, required
        instance ID
    nodeBootID : str, default is Undefined, required
        node boot ID
    nodeName : str, default is Undefined, required
        ObjectName is a Kubernetes object name represented as a DNS subdomain.
    observedAt : str, default is Undefined, required
        observed at
    """
    instanceID: str

    nodeBootID: str

    nodeName: str

    observedAt: str

    check:
        len(instanceID) <= 128
        len(instanceID) >= 1
        len(nodeBootID) <= 128
        len(nodeBootID) >= 1
        len(nodeName) <= 253
        len(nodeName) >= 1
        _regex_match(str(nodeName), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")

schema NetworkingWaycloakIoV1beta1VPNWorkloadBindingStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """
    lastTransitionTime: str

    message: str

    observedGeneration?: int
    reason: str

    status: "True" | "False" | "Unknown"

    $type: str

    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")
